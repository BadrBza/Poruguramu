@page
@model Puroguramu.App.Pages.Lecons.LessonDetail
@{
    Layout = "_Layout";
}

<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Détail de la Leçon</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <script src="~/js/dropdown.js"></script>
</head>
<body class="bg-gray-900 text-gray-200 flex flex-col min-h-screen">



<div class="flex-grow container mx-auto p-10">
    <h1 class="text-3xl font-bold mb-6">@Model.Lesson.Title</h1>
    <p class="text-gray-400 mb-4">@Model.Lesson.Description</p>

    <!-- Liste des exercices -->
    <div class="space-y-4">
        @foreach (var exercise in Model.Exercises)
        {
        <div class="bg-gray-800 rounded-xl p-6 shadow-lg">
            <h2 class="text-xl font-semibold">@exercise.Title</h2>
            <p class="text-gray-400">Difficulté : @exercise.DifficultyExo</p>
            <p class="text-gray-400">État : @exercise.Status</p>
            <a href="/Exercices/Exercice?exerciseTitle=@exercise.Title&exerciseStatus=@exercise.Status"
               class="inline-block px-4 py-2 bg-blue-500 text-white font-medium text-sm rounded-md shadow-sm hover:bg-blue-700 hover:shadow-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-opacity-50 transition duration-200 ease-in-out mt-4">Commencer
                cet exercice</a>
        </div>
        }
    </div>
</div>

</body>
</html>